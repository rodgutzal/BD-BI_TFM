# Development and testing dependencies
# Install with: pip install -r requirements-dev.txt
#
# BD_BI_TFM: auditado contra cada import real del proyecto (ver tabla en
# README.md, sección "Qué necesitas instalar para cada cosa"). Cambios
# respecto al requirements-dev.txt original de urban-mobility-analytics1:
#   - Se agregaron xgboost y prophet: src/models/ensemble_predictor.py los
#     importa con try/except (el ensemble funciona sin ellos, pero usando
#     menos modelos), y no estaban declarados en ningún requirements.
#   - dowhy: NINGÚN archivo del proyecto lo importa (ni siquiera
#     causal_analysis.py, que implementa su propia inferencia causal con
#     sklearn/scipy). Se deja comentado como punto de extensión futuro, no
#     como dependencia real.

# Core dependencies (from requirements.txt)
-r requirements.txt

# Testing
pytest>=7.0
pytest-cov>=4.0
pytest-xdist>=3.0
pytest-asyncio>=0.21.0   # no hay tests async todavía, pero src/api.py sí es async (FastAPI)

# Code quality (usados por .github/workflows/tests.yml)
black>=23.0
isort>=5.0
pylint>=2.0
flake8>=6.0
mypy>=1.0

# API development (necesarios para correr src/api.py: uvicorn src.api:app)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0

# ML ensemble opcionales (src/models/ensemble_predictor.py los detecta con
# try/except — sin ellos, el ensemble usa solo Random Forest + Gradient
# Boosting; con ellos, suma XGBoost y Exponential Smoothing)
xgboost>=2.0
prophet>=1.1

# Documentation
sphinx>=6.0
sphinx-rtd-theme>=1.3

# Development tools
ipython>=8.0
jupyter>=1.0
notebook>=7.0

# Punto de extensión futuro, NO usado por ningún módulo actual:
# dowhy>=0.11  # Causal inference library

# Monitoring and profiling
py-spy>=0.3
memory-profiler>=0.61
